spring.application.name=payment-service
server.port=8084

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/b2b_marketplace?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=1234
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# CORS Configuration
cors.allowed-origins=http://localhost:3000,http://localhost:3001,http://localhost:3002,http://localhost:5173

# Frontend URL (for Stripe redirect URLs)
app.frontend.url=http://localhost:3000

# ==================== Stripe Configuration ====================
# Get your keys from: https://dashboard.stripe.com/apikeys
# Set these as environment variables for security
stripe.secret.key=${STRIPE_SECRET_KEY:sk_test_placeholder}
stripe.public.key=${STRIPE_PUBLIC_KEY:pk_test_placeholder}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET:whsec_placeholder}

# ==================== Razorpay Configuration ====================
# Get your keys from: https://dashboard.razorpay.com/app/keys
razorpay.key.id=rzp_test_RwLvM9vyTCazm9
razorpay.key.secret=cA79yBxLEzImwpacxbq7WC0L

# Legacy Payment Gateway Configuration (Demo values)
payment.stripe.api-key=sk_test_your_stripe_key
payment.stripe.webhook-secret=whsec_your_webhook_secret
payment.paypal.client-id=your_paypal_client_id
payment.paypal.client-secret=your_paypal_client_secret
payment.paypal.mode=sandbox
